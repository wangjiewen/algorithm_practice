
dfs(region, location) {
	if (location ok) {
		mark location;
		dfs(regin, next location);
		dfs(regin, next location);
		...
		return conditions are met;
	}
	return conditions aren't meet;
}

bfs()
{
	queue;
	while (queue isn't empty) {
		take one form queue;
		if (location ok) {
			mark or change;
			add next location to queue;
			add next location to queue;
			...
		}
	}
}
