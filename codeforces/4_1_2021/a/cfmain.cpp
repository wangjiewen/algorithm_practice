#include <bits/stdc++.h>

using namespace std;

void solve()
{
	char str[10000];
	while (scanf("%s", str) != EOF && str && isalpha(str[0])) {
		printf("NO\n");
	}
}

int main(void)
{
	freopen("in", "r", stdin);

	solve();

	return 0;
}

